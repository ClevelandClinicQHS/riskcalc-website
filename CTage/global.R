#CT Age, this file is run one time when app is launched, load global objects

## source the script

load("models.RData")

## when the models are changed, have them run this in console: save(model1, model2, file = "models.RData")